<%= form_for [@question, @answer] do |f| %>
  <%= f.label :content, class: 'answer-labels' %>
  <%= f.text_area :content %><br>

  <button type="submit" class="btn btn-default">Submit</button>
<% end %>
