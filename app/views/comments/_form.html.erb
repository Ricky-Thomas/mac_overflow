<%= form_for [@answer, @comment] do |f| %>
  <%= f.label :content, class: 'comment-labels' %>
  <%= f.text_area :content %><br>

  <button type="submit" class="btn btn-default">Submit</button>
<% end %>
