<%= form_for @question do |f| %>

  <%= f.label :title %><br>
  <%= f.text_area :title %><br>

  <%= f.label :prompt %>
  <%= f.text_area :prompt %>

  <button type="submit" class="btn btn-default">Submit</button>
<% end %>
